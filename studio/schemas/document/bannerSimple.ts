import bannerText from "../includes/bannerText"
import simpleBlockContent from "../includes/simpleBlockContent"

export default {
    name: 'bannerSimple',
    title: 'Simple Banner',
    type: 'document',
    fields: [
        bannerText,
        simpleBlockContent,
    ],
}